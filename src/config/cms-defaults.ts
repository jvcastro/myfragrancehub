/**
 * Fallback site copy when CMS (`SiteSetting`) has not been saved yet, or a text field is empty.
 * Replace via **Admin → Site settings** in production.
 */
export const defaultAboutMarkdown = `## Our approach

We are a curated fragrance studio: every bottle in the catalog is chosen for composition, longevity on skin, and coherence with the rest of the collection.

## How we work

We do not use anonymous checkout. Browse the collection, then reach out through the contact options on this site—we confirm availability, batch details, and **pricing in Philippine peso (PHP)** personally before anything is reserved.

## Visit us

Use Admin → Site settings to add your brand story, contact email, Messenger link, and social profiles. This page will then reflect your live copy.`;
