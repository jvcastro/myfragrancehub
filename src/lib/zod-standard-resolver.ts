/**
 * Zod 4 implements Standard Schema; use this instead of `@hookform/resolvers/zod`
 * (which targets Zod 3 types).
 */
export { standardSchemaResolver } from "@hookform/resolvers/standard-schema";
